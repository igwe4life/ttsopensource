# GPU inference service dependencies.
# Pin loosely (major versions) — exact pins belong in a lockfile generated on
# the actual target GPU image (CUDA version dependent for torch/ctranslate2).

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
websockets>=12.0
python-multipart>=0.0.9

# STT
faster-whisper>=1.0
openai-whisper>=20231117

# Translation (NLLB + Marian/OPUS-MT both load via transformers)
transformers>=4.40
sentencepiece>=0.2.0
sacremoses>=0.1.1

# TTS
coqui-tts>=0.24        # XTTS v2 (`import TTS` — package name is coqui-tts on PyPI)
# Piper's own Python package (piper1-gpl project) — loads the ONNX voice
# model ONCE and keeps it resident for repeated synthesize() calls, unlike
# the old standalone-binary approach (subprocess spawn + full model reload
# on every single call, which saturated CPU at just 3 concurrent languages
# in real testing). Bundles espeak-ng for phonemization, no separate system
# package needed.
piper-tts>=1.2

# Shared
torch>=2.2             # install the CUDA build matching your image (see Dockerfile)
numpy>=1.26
soundfile>=0.12

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
